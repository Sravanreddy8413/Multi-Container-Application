
    - name: Install Docker, Git, and Nginx
      dnf:
        name:
          - docker
          - git
          - nginx
        state: present

    - name: Download Docker Compose standalone binary
      get_url:
        url: https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: Ensure docker-compose command symlink exists
                                                                                                                             58,18         73%
    - name: Install Docker, Git, and Nginx
      dnf:
        name:
          - docker
          - git
          - nginx
        state: present

    - name: Download Docker Compose standalone binary
      get_url:
        url: https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: Ensure docker-compose command symlink exists
                                                                                                                             58,18         73%
    - name: Install Docker, Git, and Nginx
      dnf:
        name:
          - docker
          - git
          - nginx
        state: present

    - name: Download Docker Compose standalone binary
      get_url:
        url: https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: Ensure docker-compose command symlink exists
                                                                                                                             58,18         73%
    - name: Install Docker, Git, and Nginx
      dnf:
        name:
          - docker
          - git
          - nginx
        state: present

    - name: Download Docker Compose standalone binary
      get_url:
        url: https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: Ensure docker-compose command symlink exists
                                                                                                                             58,18         73%
    - name: Install Docker, Git, and Nginx
      dnf:
        name:
          - docker
          - git
          - nginx
        state: present

    - name: Download Docker Compose standalone binary
      get_url:
        url: https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: Ensure docker-compose command symlink exists
                                                                                                                             58,18         73%
    - name: Install Docker, Git, and Nginx
      dnf:
        name:
          - docker
          - git
          - nginx
        state: present

    - name: Download Docker Compose standalone binary
      get_url:
        url: https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: Ensure docker-compose command symlink exists
                                                                                                                             58,18         73%---
- name: Configure Production Server for Todo App
  hosts: todo_server
  become: yes
  tasks:
    - name: Update all packages
      dnf:
        name: "*"
        state: latest

    - name: Install required system packages
      dnf:
        name:
          - git
          - docker
          - nginx
        state: present

    - name: Download Docker Compose standalone binary
      get_url:
        url: https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: Start and enable Docker service
      systemd:
        name: docker
        state: started
        enabled: yes

    - name: Add ec2-user to docker group
      user:
        name: ec2-user
        groups: docker
        append: yes

    - name: Start and enable Nginx service
      systemd:
        name: nginx
        state: started
  cd ~/nodejs-mongodb-todo/ansible
  enabled: yes

  ---
- name: Configure Todo API Server
  hosts: todo_server
  become: true

  tasks:
    - name: Update DNF/YUM packages
      dnf:
        name: "*"
        state: latest

    - name: Install Docker, Git, and Nginx
      dnf:
        name:
          - docker
          - git
          - nginx
        state: present

    - name: Download Docker Compose standalone binary
      get_url:
        url: https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: Ensure docker-compose command symlink exists
      file:
        src: /usr/local/bin/docker-compose
        dest: /usr/bin/docker-compose
        state: link

    - name: Start and enable Docker service
      service:
        name: docker
        state: started
        enabled: true

    - name: Add ec2-user to docker group
      user:
        name: ec2-user
        groups: docker
        append: trueansible-p


    - name: Install Docker, Git, and Nginx
      dnf:
        name:
          - docker
          - git
          - nginx
        state: present

    - name: Download Docker Compose standalone binary
      get_url:
        url: https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: Ensure docker-compose command symlink exists
    - name: Install Docker, Git, and Nginx
      dnf:
        name:
          - docker
          - git
          - nginx
        state: present

    - name: Download Docker Compose standalone binary
      get_url:
        url: https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: '0755'

    - name: Ensure docker-compose command symlink exists
                                                                                                                             58,18         73%
                                                                                                                             58,18         73%
